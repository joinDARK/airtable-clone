import TableConfig from "../../classes/TableConfig";
import columns from "../columns/columnsContractor";

const contragents = new TableConfig("Контрагенты", "contragents", columns, "Контрагенты");

export default contragents;
