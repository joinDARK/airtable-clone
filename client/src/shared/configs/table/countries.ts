import TableConfig from "../../classes/TableConfig";
import columns from "../columns/columnsCountry";

const countries = new TableConfig("Страны", "countries", columns, "Страны");

export default countries;
