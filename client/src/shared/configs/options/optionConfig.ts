import IOptions from "@interfaces/IOptions"
import * as options from "./options"

export const optionsConfig: IOptions = {
  status_swift: options.status_swift,
  type_transaction: options.type_transaction,
  calc_condition: options.calc_condition,
  currency: options.currency,
  status: options.status,
  stage_problem: options.stage_problem,
  mistake_is_it_name: options.mistake_is_it_name,
  stuck_money_name: options.stuck_money_name
}